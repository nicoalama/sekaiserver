# https://github.com/gitleaks/gitleaks

title = "Sekai Link secret scan"

[allowlist]
paths = [
  # Auto-generated / vendored
  "node_modules",
  ".next",
  ".git",
  "package-lock.json",
  "pnpm-lock.yaml",
  # Placeholder values
  ".env.example",
]
